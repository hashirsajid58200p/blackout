# Agent Rules

- **Git Commit Workflow**: After completing a task or major milestone, stage and commit the changes to Git (`git add` and `git commit`). Do **not** run `git push`; the user will push to GitHub manually once the project is complete.
